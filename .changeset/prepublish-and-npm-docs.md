---
"ideas-cli": patch
---

Run `npm run build` in `prepublishOnly` so published packages include `dist` and the `ideas` bin works after install. Document installing from npm as `ideas-cli` (the `ideas` name is used by another package on npm).
